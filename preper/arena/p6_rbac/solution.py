def can_access(user, page, config):
    """Return True if `user` may access `page` under `config`. See INSTRUCTIONS.md.

    user:   {"name": str, "roles": [role, ...]}
    config: {"role_pages": {role: [page, ...]}}
    """
    # TODO: implement
    raise NotImplementedError
